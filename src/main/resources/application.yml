spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:file:~/spock_workshop
  jpa:
      hibernate:
        ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2

logging:
  config: classpath:log-config.xml
